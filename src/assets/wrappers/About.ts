import styled from "styled-components"

const Wrapper = styled.aside`
  p {
    //styleName: Text / Para Large;
    font-family: Neue Montreal;
    font-size: 27px;
    font-weight: 400;
    line-height: 37.8px;
    text-align: left;
  }
`

export default Wrapper
